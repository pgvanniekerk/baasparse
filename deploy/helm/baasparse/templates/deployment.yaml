apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "baasparse.fullname" . }}
  labels: {{- include "baasparse.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "baasparse.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: server
  template:
    metadata:
      labels:
        {{- include "baasparse.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: server
      annotations:
        # Prometheus scrapes each pod's /metrics (OTel Prometheus exporter).
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ .Values.port | quote }}
        prometheus.io/path: "/metrics"
        # Re-roll pods when config/secret change.
        checksum/config: {{ include (print $.Template.BasePath "/config.yaml") . | sha256sum }}
    spec:
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
        {{- if and .Values.posixData.enabled .Values.posixData.runAsUser }}
        # Match the hostPath owner so app-created files are writable from the host
        # (kubelet does not apply fsGroup to hostPath volumes).
        runAsUser: {{ .Values.posixData.runAsUser }}
        runAsGroup: {{ .Values.posixData.runAsUser }}
        fsGroup: {{ .Values.posixData.runAsUser }}
        {{- end }}
      containers:
        - name: baasparse
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["--auto-migrate=false"]     # schema applied by the pre-install migrate Job
          ports:
            - name: http
              containerPort: {{ .Values.port }}
          env:
            # Instance identity = pod name → distinct claim/lease identity per replica.
            - name: BAASPARSE_INSTANCE_ID
              valueFrom:
                fieldRef: { fieldPath: metadata.name }
            - name: TMPDIR
              value: /tmp
            {{- if and .Values.posixData.enabled .Values.posixData.runAsUser }}
            # When we run as a non-image uid (to match the hostPath owner), the
            # image's $HOME (/home/nonroot) is owned by the image uid and unreadable,
            # which makes the config-file read fail. Point HOME at the writable
            # scratch dir; runtime config comes from the environment regardless.
            - name: HOME
              value: /tmp
            {{- end }}
            # GOMEMLIMIT = the container memory limit (bytes) → GC respects the cgroup.
            - name: GOMEMLIMIT
              valueFrom:
                resourceFieldRef:
                  containerName: baasparse
                  resource: limits.memory
                  divisor: "1"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "baasparse.secretName" . }}
                  key: {{ .Values.database.existingSecretKey }}
            {{- if .Values.secretKey }}
            - name: BAASPARSE_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "baasparse.fullname" . }}
                  key: BAASPARSE_SECRET_KEY
            {{- end }}
            {{- if eq .Values.storage.backend "s3" }}
            - name: BAASPARSE_S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "baasparse.fullname" . }}
                  key: BAASPARSE_S3_SECRET_KEY
            {{- end }}
          envFrom:
            - configMapRef: { name: {{ include "baasparse.fullname" . }} }
          livenessProbe:
            httpGet: { path: /healthz, port: http }
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet: { path: /readyz, port: http }
            initialDelaySeconds: 3
            periodSeconds: 5
          resources: {{- toYaml .Values.resources | nindent 12 }}
          securityContext: {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if .Values.posixData.enabled }}
            - name: file-data
              mountPath: {{ .Values.posixData.mountPath | quote }}
            {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if .Values.posixData.enabled }}
        - name: file-data
          hostPath:
            path: {{ .Values.posixData.hostPath | quote }}
            type: Directory
        {{- end }}
